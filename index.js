// here im going to call transcibe for each new steam on the platform we either can use child processes or we can use 
// lambda functions that listens for new streams and when a new stream starts creates a new ECS containers than runs
// and terminates it when the stream ends

